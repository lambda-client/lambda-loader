The official loader / auto-updater for lambda client.

This can be used as a replacement for the lambda mod itself; instead, automatically downloading the latest version and caching it locally until it's out of date.
